import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

const divRoot = document.querySelector('#root');

ReactDOM.render(<App />, divRoot);

// index.js simply imports the App.js Functional Component and Renders it.